class Test(x: Int) {
  // def foo(x: Int): String = "foo" * 2
  def bla(a: Int): Int = x match {
    case i =>
      val a = 32
      identity("hi" * 2)
      new Test(1)
      throw new Exception()
      i
  }
}
